# Migration uuid v7 : apps consommatrices

Opened: 2026-09-02
Updated: 2026-09-02
Author: agent:addon:ai/editor

Porter les apps qui consomment les packages migrés en UUIDv7 (`int $id` + `secureId` → `id` UUIDv7, y compris la clé JSON `secureId` → `id`).

## TRADING/local/manager — FAIT (commit `06aabdf`)

- `symfony/uid` installé (composer.json / .lock / symfony.lock / config/reference.php).
- 8 entités : suppression de `HasSecureIdTrait` et `setGeneratedSecureId()`, ajout de `parent::__construct()`.
- Contrôleurs, DataResolvers, DTO, Vue, twig, traductions : `{secureId}` → `{id}`.
- `front/data/entity/*.json` et `pseudocode/entity/*.yml` régénérés
  (`pseudocode:generate:pseudocode` + `api:export:entities`). La clé
  `package: "@wexample/symfony-money"` de `currency` a été remise à la main :
  le générateur la perd parce que le vendor est bind-monté en `vendor-dev`.
- Migration `Version20260902120000` : backfill préservant les données
  (76 874 bars, 304 currencies…). Timestamp v7 tiré de `date_created` quand la
  colonne existe, sinon base fixe + ancien `id` en ms, pour garder l'ordre de
  création. `down()` est `IrreversibleMigration`.
- Vérifié : `doctrine:schema:validate` OK (mapping + base), compte de lignes
  identique, zéro orphelin sur les 5 FK, tous les UUID en version 7 et
  distincts, timestamps embarqués = `date_created`, `tsc --noEmit` sans erreur
  applicative, hydratation Doctrine en `UuidV7`, `GET /api/currency/list` → 200.
- Dump avant migration : `/tmp/trader-before-uuid.sql`.

### Constaté au passage, hors périmètre
Toutes les pages HTML de trader-manager renvoient 500, avant comme après :
`MenuExtension.php:102` appelle `path()` avec `null` parce que
`AbstractController::findIndexRoute()` ne trouve pas de route index pour
`Wexample\SymfonyDesignSystem\Controller\Pages\DesignSystem`. Sans rapport avec
l'identité, non corrigé.

## MOJOE/local/design-system — rien à faire

## SYRTIS — passé à son propre journal, pas commencé

SYRTIS n'était pas prévu dans cette session. Le travail y est instruit mais non
entamé, dans `SYRTIS/local/api/.wex/journal/todo/migration-uuid-v7.md`
(commit `cc5c7b2e` du dépôt api) : périmètre complet (api, registry, core,
manager, et les clients PHP / JS / Python), constat que les 62 tables changent de
clé primaire et pas seulement les 37 qui ont un `secure_id`, et les trois
décisions à prendre avant d'écrire — sort de la donnée de production, fenêtre de
coupure, identifiants persistés hors Postgres.

Cette tâche-ci est close : ce qui restait vit désormais dans le journal de SYRTIS.
